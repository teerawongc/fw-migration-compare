{
  "name": "fw-migration-compare",
  "version": "1.0.0",
  "description": "FW Migration Comparator — CP vs FMC",
  "scripts": {
    "test": "node test/logic.test.mjs && node test/ui.test.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
